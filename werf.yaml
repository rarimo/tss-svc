configVersion: 1
project: "backend"
---
image: builder
from: golang:1.18-alpine
docker:
  WORKDIR: /go/src/gitlab.com/rarimo/tss/tss-svc
git:
  - add: /
    to: /go/src/gitlab.com/rarimo/tss/tss-svc
    stageDependencies:
      install:
        - go.mod
        - go.sum
      setup:
        - "**/*"
shell:
  install:
    - apk add git build-base
    - git config --global url."https://gitlab-ci-token:{{ env "CI_JOB_TOKEN" }}@gitlab.com/".insteadOf https://gitlab.com/
    - echo "machine gitlab.com login gitlab-ci-token password {{ env "CI_JOB_TOKEN" }}" > ~/.netrc
    - go env -w GOPRIVATE=gitlab.com/*
    - go env -w GONOSUMDB=gitlab.com/*
    - go env -w GONOPROXY=gitlab.com/*
    - cd /go/src/gitlab.com/rarimo/tss/tss-svc
    - go mod tidy
    - go mod vendor
  setup:
    - export CGO_ENABLED=1
    - export GO111MODULE=off
    - export GOOS=linux
    - go build  -o /usr/local/bin/tss-svc /go/src/gitlab.com/rarimo/tss/tss-svc

---
image: service
from: alpine:3.14.6
docker:
  ENTRYPOINT: tss-svc
shell:
  setup:
    - apk add --no-cache ca-certificates
import:
  - image: builder
    add: /usr/local/bin/tss-svc
    to: /usr/local/bin/tss-svc
    after: setup
